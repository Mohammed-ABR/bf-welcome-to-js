'use strict';

/* Strings

TODO: create a few variables(strings):
- you need to add comments to show where the variable been
declared, initialized, read , reassigned,...
*/


// Declare a variable without initializing it
let message; 

console.log(message); // Read the variable (it will print undefined)

// Initialize the variable with a string value
message = "Hello, world!"; 

console.log(message); // Read the variable (it will print "Hello, world!")

// Reassign the variable with a new value
message = "JavaScript is awesome!"; 

console.log(message); // Read the variable after reassignment (it will print "JavaScript is awesome!")
