'use strict';

/* Booleans

TODO: create a few variables(boolean):
- you need to add comments to show where the variable been
declared, initialized, read , reassigned,...
*/


// 1. Declare a variable without initializing it
let isActive; // The variable is declared but not initialized (undefined)

console.log(isActive); // Read the variable (it will print undefined)

// 2. Initialize the variable with a boolean value
isActive = true; // The variable is now initialized with the value true

console.log(isActive); // Read the variable (it will print true)

// 3. Reassign the variable with a new boolean value
isActive = false; // The variable is reassigned to a new value

console.log(isActive); // Read the variable after reassignment (it will print false)